#include <stdio.h>
#include <stdlib.h>

int main() {
    int i = 0;
    while ( i == 0) {
        char q;
        printf("\t(s) Start or (q) Quit\n");
        scanf("%c", &q);
        print_new_board();
        
    return 0;
}